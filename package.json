{
  "name": "gitspec",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "bun test",
    "typecheck": "bun run --filter '*' typecheck",
    "build": "bun packages/render/src/cli.ts",
    "preview": "bun packages/render/src/serve.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2"
  }
}
